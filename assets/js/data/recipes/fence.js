/*
 * A receita não possui desenho próprio.
 * O ícone da saída vem do catálogo do item e, quando for Canvas,
 * é renderizado pelo ItemRenderer.
 */
export const FENCE_RECIPE={id:"fence",name:"Cerca de madeira",cost:[["wood",20]],output:["fence",1]};